Uses an Arduino to encrypt the passwords and store it in a MongoDB backend server
Run npm install followed by npm start to run the project
Make sure you have MongoDB installed in your system to store the data and that the service is running in the background
